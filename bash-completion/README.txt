bash-completion for japanese file names with C/Migemo.

make:
  make all

install:
  make install

NOTE:
  install directory is defined in Makefile.
  defualt is ~/.migemo/
  also, some temporal files will be created in the save directory.

  charset of dictionary is utf-8.

